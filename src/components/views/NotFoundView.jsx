import { Typography } from "@mui/material";
import React from "react";

export const NotFoundView = () => {
  return (
    <Typography>
      {"The webpage you are trying to access was not found (404)"}
    </Typography>
  );
};
